
07-SpringMVC前后台的数据传递课后作业

  1、前后台数据传递（自行采用实现方式）
    ⑴ 使用SpringMVC进行用户（用户名，用户密码）登录
    ⑵ 要求
      ① 登录时输入用户名为"阿沙"，密码为"123456"的值时跳转到登录成功页面，并显示登录的用户名和密码，否则跳转到登录失败页面

  2、使用JSON进行数据传递
    ⑴ 改造上一个作业，使用SpringMVC和Ajax及Json数据结构进行用户（用户名，用户密码）登录
    ⑵ 要求
      ① 登录时输入用户名为"阿沙"，密码为"123456"的值时弹出"阿沙"用户登录成功提示，否则弹出登录失败提示
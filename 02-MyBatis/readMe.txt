
02-MyBatis基础操作课后作业

  1、单表简单查询
    ⑴ 查询学生信息表student指定记录
      ① 表名student（学生信息表），2个字段
         no 学号 字符串 10位 主键
         name 姓名 字符串 8位 非空
    ⑵ 要求
      ① 使用MyBatis框架编程，MySQL数据库
      ② 查询该表学号为"1001"、姓名为"阿沙"的记录，把查询出来的学号输出
      ③ 数据库的连接信息放在单独的属性文件properties中

  2、单表增删改基本操作
    ⑴ 在学生信息表student依次进行如下操作
      ① 表名student（学生信息表），2个字段
         no 学号 字符串 10位 主键
         name 姓名 字符串 8位 非空
    ⑵ 要求
      ① 使用MyBatis框架编程，MySQL数据库
      ② 要求事务管理
      ③ 在该表中增加学号为"1006"，姓名为"李四"的记录
      ④ 然后修改"1006"号学生的姓名为"李斯"
      ⑤ 最后删除"李斯"这条记录。

  3、使用接口配置增删改的数据库执行语句完成对应的基本操作
    ⑴ 在学生信息表student依次进行如下操作
      ① 表名student（学生信息表），2个字段
         no 学号 字符串 10位 主键
         name 姓名 字符串 8位 非空
    ⑵ 要求
      ① 使用MyBatis框架编程，MySQL数据库
      ② 要求事务管理，要求用接口配置SQL的方式
      ③ 在该表中增加学号为"1006"，姓名为"李四"的记录
      ④ 然后修改"1006"号学生的姓名为"李斯"
      ⑤ 最后删除"李斯"这条记录。